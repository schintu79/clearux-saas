import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Contact ClearUX — Get Support & Enterprise Inquiries',
  description: 'Have questions about ClearUX? Contact our team for support, enterprise pricing, or partnership inquiries.',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
